 <!--  Widget Properties Filter  -->
<DockPanel Grid.Row="2">
    <guide:SearchBox
        Name="AvailableFeaturesFilter"
        HintText="Filter Widget features"
        Text="{Binding Path=FilterText, Mode=TwoWay, 
        UpdateSourceTrigger=PropertyChanged, Delay=100}"/>
</DockPanel>